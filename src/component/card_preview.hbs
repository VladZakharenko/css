{{#with data}}
<div class="preview">
    <div class="preview_header">
        <span class="preview_data">{{date}}</span>

        {{#each category}}
        <span class="preview_categoty preview_categoty_{{id}}">{{text}}</span>
        {{/each}}

    </div>
    <strong class="preview_title">{{title}}</strong>
    <p class="preview_description">{{text}}</p>
    <a href="#">cont</a>
</div>
{{/with}}